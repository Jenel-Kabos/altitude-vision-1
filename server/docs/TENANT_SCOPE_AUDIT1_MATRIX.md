# TENANT-SCOPE-AUDIT-1 — Matrice d'audit

Inventaire exhaustif : 34 fichiers utilisant `tenantScopeUserIds`/`scopeUserIds` (recherche `grep -rl` sur `controllers/`, `services/`, `routes/`, `middleware/`, hors `__tests__/`). Classification stricte par occurrence significative.

| Fichier | Fonction | Acteur | Ressource | Champ comparé | Scope actuel | Public-signup concerné ? | Bug reproduit ? | Risque cross-tenant | Action |
|---|---|---|---|---|---|---|---|---|---|
| `controllers/userController.js` | `getAllUsers`/`getAllOwners` | Admin (staff) | User | `User._id` | Étendu (`expandScopeWithUnaffiliatedUsersIfSoleTenant`) | Oui | Oui — **corrigé HOTFIX-USERS-COUNT-1** | Testé, sûr (gate tenant unique) | KEEP (déjà fixé) |
| `routes/userRoutes.js` | `router.param('id', …)` | Admin (staff) | User (actions `/:id/*`) | `User._id` | Étendu (même fonction) | Oui | Oui — **corrigé HOTFIX-OWNER-CONTRACT-RESEND-1** | Testé, sûr | KEEP (déjà fixé) |
| `controllers/rentalContractRegularizationController.js` | `list`/`decide`/`revert` | Staff (Admin/GestionnaireImmobilier/Collaborateur) | Contrat (via `proprietaire.user`) | `proprietaire.user` | Étendu (même fonction) | Oui | Oui — **corrigé MICRO-HOTFIX-RENTAL-REG-SCOPE-1** | Testé, sûr | KEEP (déjà fixé) |
| `controllers/propertyPortfolioController.js` | `list` | Staff (`STAFF_IMMO`, route privée `/api/properties/portfolio`) | Property | `Property.owner` | Brut → **étendu par ce sprint** | Oui | **Oui — reproduit par test AVANT correction (1/4 échec)** | Testé cross-tenant explicitement, sûr | **FIX LOCAL (ce sprint)** |
| `services/propertyPortfolioService.js` | `getPropertyPortfolio` | (idem, appelé par le contrôleur) | Property/Accommodation/Hotel | `owner` (paramètre reçu) | Reçoit le scope du contrôleur, inchangé | — | — | Inchangé (aucune modification du service) | KEEP (aucune modification nécessaire — le service reste agnostique de la provenance du scope) |
| `controllers/rentalManagementController.js` | `list` | Staff (Admin/GestionnaireImmobilier, route privée) | RentalManagement | `RentalManagement.owner` | Brut → **étendu par ce sprint** | Oui | **Oui — reproduit par test AVANT correction** | Testé cross-tenant explicitement, sûr | **FIX LOCAL (ce sprint)** |
| `controllers/rentalManagementController.js` | `stats` | Staff (idem) | Property (agrégat) | `Property.owner` | Brut → **étendu par ce sprint** | Oui | **Oui — reproduit par test AVANT correction** | Testé cross-tenant explicitement, sûr | **FIX LOCAL (ce sprint)** |
| `routes/rentalManagementRoutes.js` | `router.param('id', …)` | Staff + Proprietaire self-service | RentalManagement (`/:id/*`) | via `assertResourceTenantOrUnattributed` | Déjà "fail-open" sur `unresolved` | Oui, déjà couvert | Non — déjà correct par construction (variante "OrUnattributed") | Déjà certifié (TENANT-CERT-2) | CORRECT AS-IS |
| `controllers/documentController.js` | `tenantDocumentFilter` (`getAllDocuments`, liste) | Staff | Document | `createdBy`/`client`/`relatedProperty→owner` | Brut, mais `$or` avec `tenant` explicite | Oui (documents legacy `tenant:null`) | NON CONFIRMÉ (pas de reproduction ciblée sur la LISTE — seule la lecture par ID a été prouvée, voir ligne suivante) | NON CONFIRMÉ | NON CONFIRMÉ — nécessiterait son propre test dédié |
| `controllers/documentController.js` | `getDocument`/`updateDocument`/`deleteDocument`/`createDocument` (par ID) | Staff | Document | via `assertResourceTenant` → `fromUser` (`OrgMembership`-only) | Brut (couche `tenantResourceAttributionService`) | Oui | **Oui — reproduit par test (404 `TENANT_RESOURCE_NOT_FOUND`)** | NON APPLICABLE (le bug est un faux négatif, pas une fuite) | **BUG CONFIRMÉ / NON FIXÉ** — cause dans `tenantResourceAttributionService.fromUser`, partagée par ~15 types de ressources certifiées séparément ; hors blast radius acceptable pour ce sprint |
| `services/platformTenant/tenantResourceAttributionService.js` | `fromUser` (utilisée par `resolveResourceTenant`/`assertResourceTenant` pour Property, Hotel, Accommodation, Conversation, Message, FinancialDocument, Contrat, Proprietaire, Locataire, RentalManagement, Litige, Signalement, RealEstateApplication…) | (couche partagée) | (15+ types) | via `resolveAvailableTenantsForUser` | Brut (`OrgMembership`-only) | Oui, structurellement | Oui (racine du bug ci-dessus) | Non testé domaine par domaine dans ce sprint | **BUG CONFIRMÉ / NON FIXÉ** — voir ci-dessus ; toute correction ici a un rayon d'action bien supérieur à `resolveTenantScope` lui-même (déjà démontré dangereux à élargir) ; nécessite un audit dédié consommateur par consommateur |
| `services/dossier/dossierSearchService.js` | `searchDossiers` | Staff | Property/Document (recherche globale) | `owner`/`createdBy`/`client` | Brut | Oui | NON CONFIRMÉ (aucune reproduction — accès direct par `getDossier` non affecté, seule la recherche pourrait sous-remonter) | Faible (recherche, pas un accès direct) | RISQUE THÉORIQUE — omission de recherche uniquement, `getDossier` par ID reste inchangé et non gated par ce scope |
| `controllers/exportController.js` | `collectContacts` (exports CSV Devis/Contacts/Utilisateurs) | Staff (Admin export) | User/Devis/Contact | `user`/`_id` | Brut | Oui | NON CONFIRMÉ | **Élevé si mal corrigé** (export de masse, PII) — mandat explicite "Risque élevé de fuite de masse" | RISQUE THÉORIQUE — KEEP tant qu'aucune reproduction n'est apportée ; toute correction future doit d'abord établir une preuve précise, jamais un élargissement générique |
| `controllers/crmController.js` / `services/crmService.js` | `synchronizeCustomers`/`loadIdentitySources` | Staff | CrmCustomer (source d'identité User/ContactMessage/QuoteRequest/AltcomProject) | `User._id` (pour la sync uniquement) | Brut | Oui, mais seulement pour la SYNCHRONISATION (création), pas la visibilité | NON CONFIRMÉ | Faible — une fois `CrmCustomer.tenant` posé, toute lecture ultérieure (`listCustomers`, `getCustomer360`, etc.) est filtrée par `tenant` explicite, PAS par `scopeUserIds` | RISQUE THÉORIQUE — omission de synchronisation possible (un contact non affilié pourrait ne pas être importé comme `CrmCustomer`), mais AUCUN enregistrement CRM existant n'est rendu invisible par ce mécanisme |
| `controllers/dashboardAnalyticsController.js` | `sales` | Staff (dashboard analytics) | Property (agrégat ventes) | `Property.owner` | Brut | Oui | NON CONFIRMÉ | Faible (métrique de comptage, pas un accès bloquant) | RISQUE THÉORIQUE — sous-comptage de métrique possible, non testé, mêmes réserves que `erpService.computeGrowth` |
| `services/erp/erpService.js` | `computeGrowth` | Staff (dashboard ERP) | User (comptage croissance) | `User._id` | Brut | Oui | NON CONFIRMÉ | Aucun (métrique de comptage uniquement) | RISQUE THÉORIQUE — sous-comptage possible d'un KPI de croissance, pas un blocage d'accès ; KEEP conformément au mandat (§15 : "préférer KEEP si aucune reproduction réelle n'existe") |
| `services/erp/erpService.js` | `getOrganizationSummary` | Staff (dashboard ERP) | `OrgMembership` (comptage direct) | `user` (dans `OrgMembership`) | Brut, **intentionnel** | Non — la question posée est littéralement "combien de membres d'organisation", un compte non affilié n'EST PAS membre par définition | Non applicable | Aucun | **CORRECT AS-IS** — sémantique différente, pas un défaut |
| `services/hotel/hotelAccessScopeService.js` | `resolveHotelAccessScope` (Admin, hôtels legacy sans `tenant`) | Admin (staff) | Hotel (legacy `tenant:null`) | `manager`/`createdBy` | Brut, puis re-vérifié par `assertResourceTenant` strict (double garde) | Oui, théoriquement (un hôtel managé par un manager non affilié) | NON CONFIRMÉ (aucune reproduction — domaine explicitement signalé comme zone de prudence extrême par le mandat, déjà lourdement certifié F26.1-F26.3/PAY/DASH/E2E) | **Élevé si mal corrigé** — mandat explicite "Ne remplace jamais ces règles par un simple user scope élargi" | RISQUE THÉORIQUE — **délibérément non testé ni modifié dans ce sprint**, conformément à la prudence explicitement demandée ; nécessite son propre audit dédié avec la même rigueur que PLATFORM-ADMIN-CERT-1 |
| `middleware/tenantContext.js` | `createRequireTenantScope` | (couche de résolution elle-même) | — | — | — | — | — | — | LEGACY/CORE — c'est la source du scope, pas un consommateur ; hors périmètre de cet audit (déjà auditée par PLATFORM-ADMIN-CERT-1) |
| `services/platformTenant/tenantContextService.js` | `resolveTenantScope`, `getScopeUserIds` (indirect) | (couche de résolution) | — | — | — | — | — | Déjà démontré dangereux à élargir (HOTFIX-USERS-COUNT-1) | **NE JAMAIS ÉLARGIR** — confirmé, aucune modification dans ce sprint |
| `services/platformTenant/platformTenantService.js` | `getTenantOverview` | PlatformOperator (vue d'ensemble tenant) | `OrgMembership` (comptage `userCount`) | via `getScopeUserIds` | Brut, **intentionnel** (commentaire explicite : "aucun nouveau comptage métier") | Non — même raisonnement que `getOrganizationSummary` | Non applicable | Aucun | **CORRECT AS-IS** |
| `controllers/actionLogController.js` | liste des logs d'action | Staff | ActionLog (`auteur.id`) | `auteur.id` | Brut (`getScopeUserIds` direct) | Non — un auteur de log d'action est nécessairement du staff, et le staff est structurellement rattaché via `OrgMembership` (flux d'invitation, jamais public-signup — voir HOTFIX-USERS-COUNT-1) | Non applicable | Aucun | **CORRECT AS-IS** — population d'acteurs différente de celle affectée par le bug |
| `services/notificationService.js` | notifications staff | Staff (destinataires) | `staffIds` | via `resolveTenantScope` | Brut, **intentionnel** (notifications STAFF uniquement) | Non — même raisonnement que ActionLog | Non applicable | Aucun | **CORRECT AS-IS** |
| `controllers/propertyController.js` (L984-990) | notification de publication | Tous utilisateurs du tenant (destinataires) | User (notifiés) | `User._id` | Brut | Oui (des clients/propriétaires non affiliés ne reçoivent pas la notification) | NON CONFIRMÉ | Aucun (notification manquée, pas un accès bloqué) | RISQUE THÉORIQUE — omission de notification marketing, faible sévérité, non testé |
| `controllers/publicApi/publicPropertyController.js`, `publicHotelController.js`, `publicAccommodationController.js` + services associés | catalogue public (clé API) | Tiers externe (API Key) | Property/Hotel/Accommodation | `owner`/`manager` | Brut, **volontairement strict** | Oui, mais c'est EXACTEMENT le comportement voulu | Non applicable | **Déjà la cause du revert HOTFIX-USERS-COUNT-1 si élargi** | **CORRECT AS-IS — NE JAMAIS ÉLARGIR** |
| `middleware/publicApiAuth.js` | résolution `req.apiKeyTenantScope` | (couche de résolution du catalogue public) | — | — | Brut, volontaire | — | — | — | **CORRECT AS-IS — NE JAMAIS ÉLARGIR** |
| `services/reporting/domains/crmReport.js`, `immobilierReport.js`, `locationReport.js`, `services/reporting/reportingService.js` | rapports internes (Admin) | Staff | Opportunités/ventes/locations (agrégats) | `assignedTo`/`owner` | Brut | Oui | NON CONFIRMÉ | Faible (métriques de reporting, pas un accès direct à une ressource) | RISQUE THÉORIQUE — même catégorie que ERP/dashboardAnalytics, sous-comptage possible, non testé, KEEP |
| `controllers/conversationController.js` | (commentaire seul, aucun usage réel) | — | — | — | — | Non applicable | Non applicable | Aucun | **LEGACY/DEAD** — référence documentaire uniquement (déjà établi dans HOTFIX-OWNER-CONTRACT-RESEND-1 : cette route utilise `attachTenantContext`, jamais bloquant) |

## Légende Action

- **KEEP (déjà fixé)** : corrigé lors d'un sprint précédent, non retouché ici, re-vérifié vert.
- **FIX LOCAL (ce sprint)** : bug reproduit par test AVANT correction, corrigé localement, prouvé cross-tenant sûr APRÈS correction, dans ce sprint.
- **CORRECT AS-IS** : lecture de code + raisonnement démontrent qu'il n'y a pas de faux négatif pour un public-signup légitime (population d'acteurs différente, sémantique de comptage différente, ou garde déjà "fail-open" par construction).
- **BUG CONFIRMÉ / NON FIXÉ** : faux négatif prouvé par test, mais la correction nécessiterait de toucher une couche partagée à trop large rayon d'action pour ce sprint — documenté pour suivi dédié.
- **RISQUE THÉORIQUE** : lecture de code suggère un gap possible, mais aucune reproduction par test n'a été apportée dans ce sprint (faute de temps/prudence explicite du mandat) — classé honnêtement NON CONFIRMÉ plutôt que supposé.
- **LEGACY/DEAD** : référence non fonctionnelle, aucun impact.
