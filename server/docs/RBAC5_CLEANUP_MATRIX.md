# RBAC-5 — MATRICE DE CLEANUP

| Élément | Consumers avant | Classification | Action | Preuve | Verdict |
|---|---:|---|---|---|---|
| `client/lib/utils/staffCapabilities.js` | 0 en production ; 2 en test (`staffCapabilities.test.js` self-test, `dashboardProfiles.test.js` fixture) | DELETE — PROVEN DEAD | Supprimé | Grep exhaustif `client/` (imports directs, dynamiques, barrels) — RBAC-3 avait déjà migré ses 2 seuls consommateurs de production | **SUPPRIMÉ** |
| `client/lib/__tests__/staffCapabilities.test.js` | Testait exclusivement le mapping supprimé | DELETE — PROVEN DEAD | Supprimé | Le fichier testé n'existe plus ; aucune couverture comportementale perdue (il ne testait que `hasStaffCapability`/`CAPABILITIES_BY_ROLE` eux-mêmes, pas un comportement d'écran) | **SUPPRIMÉ** |
| `client/lib/__tests__/dashboardProfiles.test.js` (import) | Utilisait `CAPABILITIES_BY_ROLE` comme donnée de fixture pour tester `dashboardProfiles.js` | MIGRATE | Import remplacé par une fixture locale inline (`CAPABILITIES_BY_ROLE_FIXTURE`), jamais un fichier de production partagé | Test rejoué : 3/3 verts après migration | **MIGRÉ (test uniquement)** |
| `altimmo-app/src/utils/staffCapabilities.js` | 0 en production ; 1 en test (`staffCapabilities.test.js` self-test) ; 1 mention en commentaire non fonctionnelle | DELETE — PROVEN DEAD | Supprimé | Grep exhaustif `altimmo-app/src/` (imports directs, dynamiques, barrels) — RBAC-4 avait déjà établi ce constat, re-prouvé ici avant suppression | **SUPPRIMÉ** |
| `altimmo-app/src/utils/__tests__/staffCapabilities.test.js` | Testait exclusivement le mapping supprimé | DELETE — PROVEN DEAD | Supprimé | Idem Web — aucune couverture comportementale perdue | **SUPPRIMÉ** |
| `HotelHousekeepingScreen.jsx:30` (commentaire) | Mentionnait `staffCapabilities.js` par son nom | — | Commentaire reformulé pour ne plus référencer un fichier supprimé | Lecture directe | **MIS À JOUR** (documentation seule, aucun comportement changé) |
| `STAFF_DOC` / `ROLES_PAIEMENTS` / `ROLES_DOCS` (`server/utils/roles.js`) | 3 constantes déclarées séparément avec la même valeur d'ensemble (`['Admin','Secretaire','Collaborateur']`, ordres différents) | MIGRATE | Dédupliquées en alias stricts de `CANONICAL_DOC_STAFF_ROLES`, nom nouveau introduit uniquement pour la référence partagée | `rolesAliasParity.test.js` étendu (3 nouveaux tests), suite unit 128/128, Mongo ciblé 7/7 suites (63/63 tests) sur les domaines documents/paiements/GL | **MIGRÉ (dédupliqué, noms conservés)** |
| `STAFF_IMMO` / `ROLES_ALTIMMO` / `ROLES_GL` / `ROLES_LITIGES` | Alias déjà dédupliqués en RBAC-2 vers `CANONICAL_IMMO_STAFF_ROLES` | KEEP — BUSINESS SEMANTIC | Aucune (re-vérifié : chaque nom a des consommateurs actifs et distincts par domaine) | Grep RBAC-5 : chacun des 4 noms est référencé par plusieurs fichiers de domaines différents (property, hotel, GL maintenance, litiges) | **CONSERVÉ TEL QUEL** |
| `CANCEL_ROLES` (`server/controllers/paiementController.js:435`) | 1 site, `cancelReceipt` | KEEP — CONTRACT UNCLEAR (défense en profondeur) | Aucune | L'entrée `'GestionnaireImmobilier'` y est structurellement inatteignable (le guard de route `requireCapability('payments.reverse')` bloque déjà ce rôle en amont), mais le commentaire du code indique une intention explicite de double-vérification défensive ("ce contrôleur re-vérifie explicitement le sous-ensemble autorisé") — retirer cette entrée n'aurait aucun effet observable aujourd'hui mais romprait ce filet de sécurité si le guard de route venait à changer un jour ; mandat §34 impose une protection particulière sur tout ce qui touche `payments.reverse` | **CONSERVÉ TEL QUEL — décision consciente, documentée** |
| `payment.status` (nommage singulier vs `payments.*` pluriel) | Incohérence de nommage notée RBAC-2 §46c | KEEP — CONTRACT UNCLEAR | Aucune | Aucun bug prouvé, renommer casserait un contrat sans bénéfice démontré | **CONSERVÉ TEL QUEL** |
| 8 capacités déclarées mais non consommées (`clients.read`, `owners.read`, `properties.create`, `occupancy.read`, `media.read`, `media.manage`, `messages.read`, `messages.manage`) | 0 backend, 0 Web production, 0 Mobile — seulement des fixtures de test | KEEP — CONTRACT UNCLEAR | Aucune | Voir `RBAC5_CAPABILITY_USAGE_MATRIX.md` — la déclaration dans `DEFAULT_CAPABILITIES` EST le contrat (droit accordé au rôle), la supprimer serait une rétraction de permission déguisée en nettoyage | **CONSERVÉES TELLES QUELLES** |
| `DIRECTION` (`server/routes/reportingRoutes.js:13`) | 1 site, mais réellement exercé | Suspecté LEGACY, reclassé KEEP après vérification | Aucune | Vérifié fonctionnel et intentionnellement distinct de `CANONICAL_IMMO_STAFF_ROLES` (Collaborateur exclu ici, délibérément) | **CONSERVÉ TEL QUEL** |
| ~118 `restrictTo(...)` backend + ~23 checks inline | Répartis sur 45+ fichiers | Majoritairement AUTHORIZATION/TENANT/OWNERSHIP/HOTEL_SCOPED/FINANCIAL_SCOPED légitimes | Aucune conversion mécanique | Voir `RBAC5_ROLE_CHECK_CLASSIFICATION.md` | **CONSERVÉS — mandat §26-28 interdit la conversion de masse** |
| ~9 patterns `AUTHORIZATION_STAFF` Web non migrés (`isStaffImmo`, `isStaffDocs`, listes de rôles locales dans `ManageEventsPage.jsx`/`ManageAccommodationsPage.jsx`/etc.) | Voir `RBAC5_ROLE_CHECK_CLASSIFICATION.md` | AUTHORIZATION_STAFF, candidats futurs | Aucune migration | RBAC-5 n'est pas un sprint d'expansion de la migration Web (mandat, roadmap RBAC-3) | **CONSERVÉS — candidats documentés pour un sprint futur, non exécuté** |
| `canAdd` mobile (`AuthContext.jsx:240`) | 1 consommateur (`TabNavigator.jsx`) | KEEP — CONTRACT UNCLEAR | Aucune | Migration testée conceptuellement en RBAC-4 : étendrait l'accès à GestionnaireImmobilier sans preuve produit (mandat §38) | **CONSERVÉ TEL QUEL** |
| `GestionLocativePage.jsx` `canManage`/`TransactionsPage.jsx` `isAdmin` | Caractérisés RBAC-3 | Divergences connues, contrat produit ambigu | Aucune correction | Voir `RBAC3_SECURITY_MATRIX.md`, re-confirmé non modifié dans ce sprint | **CONSERVÉS TELS QUELS — recommandation de hotfix séparé maintenue** |
| Résolveurs de redirection post-login Proprietaire | 2 résolveurs indépendants (RBAC-3) | ROUTING, hors RBAC | Aucune correction | Mandat §37 : hors scope explicite | **NON TOUCHÉ — `HOTFIX-AUTH-POSTLOGIN-ROUTING-1` recommandé, non exécuté** |

## Métriques de suppression (indicatives, pas un objectif en soi)

- Fichiers supprimés : **4** (`client/lib/utils/staffCapabilities.js`, `client/lib/__tests__/staffCapabilities.test.js`, `altimmo-app/src/utils/staffCapabilities.js`, `altimmo-app/src/utils/__tests__/staffCapabilities.test.js`).
- Fichiers modifiés : `client/lib/__tests__/dashboardProfiles.test.js` (import remplacé par fixture locale), `altimmo-app/src/screens/Hotels/HotelHousekeepingScreen.jsx` (commentaire), `server/utils/roles.js` (dédup `CANONICAL_DOC_STAFF_ROLES`), `server/__tests__/rolesAliasParity.test.js` (3 tests ajoutés).
- Mappings rôle→capacités supprimés : **2** (Web + Mobile `staffCapabilities.js`).
- Constantes littérales dupliquées dédupliquées : **3** (`STAFF_DOC`/`ROLES_PAIEMENTS`/`ROLES_DOCS` → `CANONICAL_DOC_STAFF_ROLES`).
- Checks de rôle migrés vers capacité dans ce sprint : **0** (aucune conversion supplémentaire au-delà de RBAC-2/RBAC-3, conformément au mandat).
- Checks de rôle classifiés et conservés : voir `RBAC5_ROLE_CHECK_CLASSIFICATION.md` (~141 backend échantillonnés, ~14 fichiers Web).
- Capacités ajoutées : **0**. Capacités supprimées : **0**.
