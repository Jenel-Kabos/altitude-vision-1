# AUDIT-HOTEL-MODERATION-TEST-DRIFT-1 — Matrice du diff

| Modification | Catégorie | Impact métier ? | Impact sécurité ? | Impact test ? |
|---|---|---:|---:|---:|
| Import `DashboardCard/DashboardPage/DashboardPageHeader/DashboardState/DashboardToolbar` (composants déjà utilisés par `PropertyModerationPage.jsx`/`AccommodationModerationPage.jsx`) | STRUCTURE DOM | Non | Non | Oui (nouveaux wrappers DOM) |
| Stats (Total/Complets/Incomplets/Modifs proposées) en cartes dégradées + filtre en pilules (`FILTERS`) | UX / VISUEL | Non (dérivé de `hotels` déjà chargés, aucun nouvel appel réseau) | Non | Non (nouvelles fonctionnalités, aucune assertion existante n'en dépendait) |
| `error` state via `DashboardState type="error"` au lieu de `toast.error` seul | UX (état d'erreur visible en page, pas seulement une notification éphémère) | Non | Non | Non (aucun test existant sur le chemin d'erreur) |
| Grille de cartes compactes (`DashboardCard`, image de couverture, badges "Complétude"/"Modif. proposée") remplaçant les blocs `<div className="border rounded-lg p-4">` avec galerie de vignettes inline | STRUCTURE DOM / VISUEL | Non | Non | Oui — l'ancien test ne cherchait pas ces éléments |
| Bouton **"Voir les détails"** par carte + `onClick={() => setSelectedHotel(hotel)}`, actions de modération (Valider/Rejeter/Modification proposée/motif de rejet) déplacées dans une **modale de détail** au lieu d'être inline dans chaque carte | STRUCTURE DOM + ACTION (déplacement, pas suppression) | Non — mêmes actions, même déclenchement final (`handleValidate`/`handleReject`), simplement accessibles après un clic supplémentaire | Non — aucun contournement de garde, l'action reste déclenchée par le même code | **Oui — cause directe des 2 échecs** : le test cherchait "Valider" et "Modification sensible proposée" dans le DOM initial, jamais atteignables sans ouvrir la modale |
| `handleValidate`/`handleReject` : logique interne inchangée (mêmes appels `reviewHotel(id, 'validate')` / `reviewHotel(id, 'reject', {reason: rejectReason.trim()})`, même validation "motif requis", même filtrage de la liste au succès, mêmes messages toast) + ajout `closeModal()` après succès et `setRejectingId`/`finally` pour l'état de chargement du rejet (bug mineur corrigé : l'ancien code ne réinitialisait jamais `rejectingId` en cas d'erreur de rejet) | ACTION / DONNÉES | **Non** — mêmes endpoints, même payload, même transition métier | Non | Non |
| `isRejecting`/`rejectReason` : formulaire de rejet toujours à motif obligatoire, désormais dans la modale plutôt qu'inline dans la carte | ACTION | Non — même règle (motif requis avant `handleReject`) | Non | Non (règle testable si un test l'exerçait, ce qui n'était pas le cas dans l'ancien test) |
| Texte "Modification sensible proposée" et "La version actuellement publiée reste exploitée jusqu'à la décision." | LIBELLÉ | Non — texte identique au caractère près à l'ancienne version (`git diff` confirme), seulement déplacé de la carte vers la modale | Non | Oui (déplacement de position dans le DOM, pas de changement de contenu) |
| `starLabel()` helper extrait (`${n} étoile${n>1?'s':''}` vs ancien inline `${hotel.starRating} étoile(s)`) | LIBELLÉ (reformulation mineure, pluriel correct au lieu de "(s)") | Non | Non | Non (aucun test n'assertait le texte exact de la catégorie) |
| Icônes Lucide ajoutées (`Filter, Eye, Tag, Star, BedDouble, Wallet, Sparkles, AlertTriangle, Building2`) | VISUEL | Non | Non | Non |

## Conclusion de la matrice

**Aucune ligne du diff ne touche à un endpoint, une méthode HTTP, un payload, une permission, un rôle, une transition de statut métier, ou une donnée exposée.** La seule ligne à impact test réel est le déplacement des actions de modération et du texte de comparaison de version depuis la carte inline vers une modale de détail — un changement de structure DOM/UX, pas de contrat métier. Cohérent avec le même pattern déjà en place et non contesté dans `PropertyModerationPage.jsx` (modération Property) et `AccommodationModerationPage.jsx` (modération Hébergement, fichier déjà commité, non touché par ce diff) — confirmé par lecture directe de ce dernier fichier (`DashboardCard`, `onClick={() => setSelectedAccommodation(acc)}`, bouton "Voir les détails" identique).
