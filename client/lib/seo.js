// Point d'entrée principal pour les utilitaires SEO
// Import : import { buildMetadata, buildTitle, SITE_URL } from '@/lib/seo';
export {
  SITE_NAME,
  SITE_URL,
  DEFAULT_IMG,
  DEFAULT_DESC,
  buildTitle,
  buildMetadata,
} from '@/lib/utils/metadata';
